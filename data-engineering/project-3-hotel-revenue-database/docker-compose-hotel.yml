services:
  postgres:
    image: postgres:16
    container_name: hotel_postgres
    environment:
      POSTGRES_USER: mateen
      POSTGRES_PASSWORD: mateen_pw
      POSTGRES_DB: hotel_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
